文档API 参考其他API 参考复制页面Vigolium API 参考页面的完整索引，涵盖扫描、漏洞发现、数据导入、Agent 控制等端点。复制页面浏览下方完整的 Vigolium API 表面。每一行链接到专门的参考页面，包含请求/响应详情、参数和示例。  
基础 URL：`http://localhost:9002`（默认）  

## 通用

| 类别 | 描述 | 参考 |
|:-----|:-----|:-----|
| 概述 | 服务器启动、健康检查、身份验证和通用端点 | 查看 → |
| 身份验证 | Bearer token 认证、基于文件的用户系统、角色和登录 | 查看 → |

## 核心端点

| 类别 | 描述 | 参考 |
|:-----|:-----|:-----|
| 项目 | 多租户数据隔离，所有扫描数据限定在项目范围内 | 查看 → |
| 扫描 | 单目标扫描、扫描管理、历史记录、暂停/恢复和日志 | 查看 → |
| 漏洞发现 | 列出、检索和删除漏洞发现，支持过滤 | 查看 → |
| HTTP 记录 | 请求/响应记录，支持过滤、分页和排序 | 查看 → |
| 数据导入 | 通过 URL、curl 命令、HAR、OpenAPI、Burp、Postman 导入数据 | 查看 → |

## AI 与 Agent

| 类别 | 描述 | 参考 |
|:-----|:-----|:-----|
| Agent | 查询、Autopilot、Swarm 和审计运行模式，支持 SSE 流式传输和 OpenAI 兼容聊天 | 查看 → |

## 配置与扫描范围

| 类别 | 描述 | 参考 |
|:-----|:-----|:-----|
| 设置 | 查看和更新服务器配置，支持热重载 | 查看 → |
| 扫描范围 | 控制哪些 HTTP 记录在扫描范围内 | 查看 → |
| 模块 | 列出活跃和被动扫描模块，支持搜索和标签过滤 | 查看 → |

## 集成与插件

| 类别 | 描述 | 参考 |
|:-----|:-----|:-----|
| 插件 | 管理 JavaScript 和 YAML 插件，用于自定义扫描逻辑 | 查看 → |
| 存储 | 云对象存储，用于源代码上传和扫描结果归档 | 查看 → |

## 数据与诊断

| 类别 | 描述 | 参考 |
|:-----|:-----|:-----|
| 统计 | HTTP 记录、模块和漏洞发现的聚合统计 | 查看 → |
| OAST 交互 | 来自 interactsh 回调的带外安全测试交互 | 查看 → |
| 诊断 | 系统就绪检查（数据库、Agent 供应商、工具、目录） | 查看 → |
| 数据库 | 任何数据库表的统一 CRUD API，支持分页和过滤 | 查看 → |

上一页  
安全警告  
运行 Vigolium 时的重要安全考虑：Agent 模式故意不提供沙箱，插件可以执行任意命令。  

下一页  
⌘I  
website  
twitter  
github  
discord  
x  
linkedin  

技术支持  
本文档基于 Mintlify 构建和托管，Mintlify 是一个开发者文档平台  

本页内容  
通用  
核心端点  
AI 与 Agent  
配置与扫描范围  
集成与插件  
数据与诊断